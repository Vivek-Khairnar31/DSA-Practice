#include <bits/stdc++.h>
using namespace std;
void solve() {
    int n;
    cin >> n;
    string a,b;
    cin >> a >> b;
    int i = 0;
    int j = 0;
    string ans;
    ans.push_back(a[0]);
    while(true) {
        if(i == n-1 && j == 1) {
            break;
        }
        if(j == 0) {
          if(i == n-1) {

          }else if(a[i+1] == b[i]) {

          }else if(a[i+1] > b)
        }else {

        }
        

    }
}
int main() {
    int t;
    cin >> t;
    while(t--) solve();
}