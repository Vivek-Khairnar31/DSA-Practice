#include<bits/stdc++.h>
using namespace std;

void printUnorderedSet(){
   unordered_set<int> us;


   //lower bound and upper bound does not present in this set 
   //rest of all are same 
   //this stores unique but it's does not stores in sorted order 
}

int main() {
    printUnorderedSet();
    return 0;
}