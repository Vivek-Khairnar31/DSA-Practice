#include<bits/stdc++.h>
using namespace std;

void printMultiMap(){
   multimap<int,int> mulmp;

   //everything is same as map 
   //the only difference is it can store multiple keys or duplicate keys 
   //everthing in sorted order
   
}
int main() {
    printMultiMap();
    return 0;
}